import React from "react";
import Animate from "../components/animate/animate";

type Props = {};

const Contact = (props: Props) => {
  return <Animate>Contact</Animate>;
};

export default Contact;
